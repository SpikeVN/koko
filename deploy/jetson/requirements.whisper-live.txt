# WhisperLive's package metadata declares Python >=3.9, although its
# faster-whisper server runs on JetPack 5's Python 3.8. whisper_live.py and
# whisper-live.Dockerfile install it separately with --ignore-requires-python.

# Jetson's base image provides this GPU stack. Local Python 3.8 provisioning
# installs compatible wheels instead.
ctranslate2==4.4.0; platform_machine != "aarch64"
nvidia-cublas-cu12; platform_machine != "aarch64"
nvidia-cudnn-cu12>=8,<10; platform_machine != "aarch64"
torch==2.4.1; platform_machine != "aarch64" and python_version < "3.12"
torchaudio==2.4.1; platform_machine != "aarch64" and python_version < "3.12"
torch==2.3.1; platform_machine != "aarch64" and python_version >= "3.12"
torchaudio==2.3.1; platform_machine != "aarch64" and python_version >= "3.12"
numpy==1.24.4; platform_machine != "aarch64" and python_version < "3.12"
onnxruntime==1.16.3; platform_machine != "aarch64" and python_version < "3.12"
onnxruntime>=1.18; platform_machine != "aarch64" and python_version >= "3.12"

# HTTP/WebSocket runtime needed by the WhisperLive server on both targets.
tomli
websockets==11.0.3
fastapi==0.103.2
uvicorn==0.23.2
python-multipart==0.0.6
websocket-client==1.6.4
av==12.3.0
huggingface-hub==0.36.2
tokenizers==0.20.3
tqdm==4.70.1
